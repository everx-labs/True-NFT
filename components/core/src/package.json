{
  "name": "@ton-contracts/core",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "env ts-mocha -p ../../tsconfig.json -r dotenv/config -t 300000 'tests/**/*.test.ts'",
    "compile": "node ./compile.js",
    "test:compile": "npm run compile && npm run test"
  }
}
